package day14_multi_branch_if_statements;

public class ClassNotes {
    /*
    in order to produce boolean, there is multiple ways:
    1. boolean variable
    2. comparison operator
   3. string equals()method

   assign variable value based on condition
   if statements need to have a boolean expression, it doesn't mean that it has to be a boolean variable it can be something that just gives a boolean value like the == operator would

   debugging: to be able to see on the backgroun check what computer is doing/checking
    */
}
