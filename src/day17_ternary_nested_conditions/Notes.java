package day17_ternary_nested_conditions;

public class Notes {
    /*

    syntax:
    if (condition)
        if()
        else
    else if()
          if()
          else

    ternary is for very basic things.
    if you want return one thing you can use ternary but if you use more than one thing, you cant use ternary.
advantages:
one statement to replace basic if else statement
if else is more flexible when you have multi branch conditions.

NESTED IF STATEMENT:
WE CAN PUT IF statement inside IF statement.
if (condition1){
       if(condition2) {
       code
}


     */
    //pseudocode: code written in human langauge. then it will be translated to jave or other progrramming language
}

/*
String quality= "dhf";
        int rating= quality.equals("good") ? 100 : 0;
        System.out.println(rating);
        System.out.println(quality.equals("good") ? 100 : 0); direk println in icine de yazabilioruz.
 */
