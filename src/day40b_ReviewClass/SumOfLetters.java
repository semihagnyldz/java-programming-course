package day40b_ReviewClass;

/*public class SumOfLetters {
    public static void main(String[] args) {
        getSumFromString("a5bc12def100g");

    }

    public static int getSumFromString(String str) {
        int sum = 0;
        String num = ""; // bc we are building a string to hold up the numbers.

        for (int i = 0; i < str.length(); i++) {
            if (Character.isDigit(str.charAt(i)))// checks every character if it is digit (number) or not
                //str.charAt(i) //reads every character one at a time
                num += str.charAt(i);
            if (Character.isDigit(str.charAt(i + 1))) {
                sum += Integer.parseInt(num);
            }
        }
    }
}

 */








